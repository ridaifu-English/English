[project]
name = "kahoot-converter"
version = "0.1.0"
description = "Convert Kahoot CSV quiz files to Excel format"
requires-python = ">=3.8"
dependencies = [
    "pandas>=2.0.0",
    "openpyxl>=3.1.0",
]

[project.scripts]
kahoot-convert = "kahoot-convert:main"

